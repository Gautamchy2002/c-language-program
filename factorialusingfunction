#include <stdio.h>

void main()
{
    void factorial();
    factorial();
}
void factorial()
{
    int i,n,a=1;
    printf("enter the integer");
    scanf("%d",&n);
    for(i=1;i<=n;i++)
    {
        a=a*i;
    }
    printf("the factorial is %d",a);
}
