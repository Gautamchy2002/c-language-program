#include <stdio.h>

int main()
{
    char a[30];
    int i,w=0,b=0,c=0;
    printf("enter the string");
    gets(a);
    for(i=0; a[i]!='\0';i++)
    {
        c=c+1;
    }
printf("the length of string is: %d",c);
for(i=0;a[i]!='\0';i++)
{
    if(a[i]==' ')
    {
       b=b+1;
    }
     
}
printf("total no of space is %d",b);
printf("total of words is %d",b+1);
    return 0;
}
