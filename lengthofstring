#include <stdio.h>

void main()
{
    int i,c=0;
    char a[10];
    printf("enter the string");
    gets(a);
    for(i=0; a[i]!='\0';i++)
    {
        c=c+1;
    }
    printf("\n the length of string is %d",c);
}
